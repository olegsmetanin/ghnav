export const issueListValueSelector = state => state.issueList.value

export const issueListQuerySelector = state => state.issueList.query

export const issueListProcessSelector = state => state.issueList.process

export const issueListErrorSelector = state => state.issueList.error
