export const repoListValueSelector = state => state.repoList.value

export const repoListQuerySelector = state => state.repoList.query

export const repoListProcessSelector = state => state.repoList.process

export const repoListErrorSelector = state => state.repoList.error
